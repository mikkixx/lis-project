# Лабораторная информационная система (LIS)

Система для управления химическими экспериментами, образцами, оборудованием и результатами.

## 📌 Особенности

- Управление экспериментами, образцами, оборудованием, условиями и результатами
- Генерация отчётов в форматах **Excel** и **PDF**
- Ролевая модель:  
  - Просмотр **всех** экспериментов  
  - Редактирование **только своих** данных
- Графический интерфейс на **PySide6**
- Хранение данных в **MySQL** через ORM **Peewee**
- Поддержка добавления/редактирования/удаления сущностей через диалоги
- Просмотр образцов по двойному клику (без редактирования)

## 🛠️ Требования

### Системные
- **ОС**: Debian 11/12, Ubuntu 20.04+
- **Python**: 3.10 или выше
- **MySQL**: 8.0 или выше

### Зависимости Python
См. [`requirements.txt`](requirements.txt)

## 🚀 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/mikkixx/lis-project.git
cd lis-project

### 2. Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
# venv\Scripts\activate   # Windows

### 3. Установка зависимостей
pip install --upgrade pip
pip install -r requirements.txt

### 4. Настройка базы данных
a) Создайте пустую БД в MySQL
Через терминал или phpMyAdmin выполните:
CREATE DATABASE lis_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

b) Настройте подключение
cp .env.example .env
nano .env
Пример содержимого .env:
DB_HOST=localhost
DB_PORT=3306
DB_NAME=lis_db
DB_USER=root
DB_PASSWORD=

c) Создайте таблицы
python database/init_db.py

### 5. Запуск приложения
python main.py
